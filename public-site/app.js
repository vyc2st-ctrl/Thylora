const menu=document.querySelector('.menu');const nav=document.querySelector('.nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});nav.addEventListener('click',e=>{if(e.target.tagName==='A'){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');}})}

const form=document.getElementById('admissionForm');const status=document.getElementById('formStatus');if(form){form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form);const payload=Object.fromEntries(fd.entries());payload.committee_review=fd.get('committee_review')==='on';sessionStorage.setItem('thylora_business_application_draft',JSON.stringify(payload));status.textContent='Saved locally as a private application draft. Backend submission wiring is still gated until the public-site API configuration is connected and tested; nothing was published.';});}
