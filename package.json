{
  "name": "thylora",
  "private": true,
  "type": "module",
  "description": "THYLORA source: public site, member app, RAE Link media network, backend migrations.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:sleep-model": "python3 -m unittest discover -s sleep-model -t sleep-model"
  }
}
