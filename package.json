{
  "name": "thylora",
  "private": true,
  "type": "module",
  "description": "THYLORA source: public site, member app, RaeLynk media network, backend migrations.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
