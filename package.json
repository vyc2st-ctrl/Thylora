{
  "name": "thylora",
  "private": true,
  "type": "module",
  "description": "THYLORA source: public site, member app, RAE Link media network, backend migrations.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "node --test tests/e2e/*.test.mjs",
    "test:db": "db/rae-link/validation/run.sh",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
